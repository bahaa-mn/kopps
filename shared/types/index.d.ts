/* eslint-disable no-unused-vars */

export type Match = {
  id: string
}

export type Team = {
  id: string
}

export enum MatchType {
  Football = 'foot',
  Basketball = 'basket',
  Paddle = 'paddle',
}
